From 376a9a8880ad3afdad8fcfe309abb79c08b6dd39 Mon Sep 17 00:00:00 2001
From: Adam Cohen Hillel <adamcohenhillel@gmail.com>
Date: Wed, 27 May 2026 00:41:46 +0300
Subject: [PATCH 2/4] OpenPhone: fix fs_config host build on Darwin

Change-Id: I7be696d7e5a698bdf9a5335bbb27d7532dca9661

diff --git a/libcutils/Android.bp b/libcutils/Android.bp
index ec9b75493..ece977cf1 100644
--- a/libcutils/Android.bp
+++ b/libcutils/Android.bp
@@ -167,6 +167,12 @@ cc_library {
                 "fs_config.cpp",
             ],
         },
+        darwin: {
+            srcs: [
+                "canned_fs_config.cpp",
+                "fs_config.cpp",
+            ],
+        },
         host: {
             srcs: [
                 "trace-host.cpp",
diff --git a/libcutils/include/private/fs_config.h b/libcutils/include/private/fs_config.h
index 9a727bc58..1fbd87371 100644
--- a/libcutils/include/private/fs_config.h
+++ b/libcutils/include/private/fs_config.h
@@ -27,7 +27,62 @@
 #include <sys/cdefs.h>
 #include <unistd.h>
 
+#if defined(__linux__)
 #include <linux/capability.h>
+#else
+#define CAP_CHOWN 0
+#define CAP_DAC_OVERRIDE 1
+#define CAP_DAC_READ_SEARCH 2
+#define CAP_FOWNER 3
+#define CAP_FSETID 4
+#define CAP_KILL 5
+#define CAP_SETGID 6
+#define CAP_SETUID 7
+#define CAP_SETPCAP 8
+#define CAP_LINUX_IMMUTABLE 9
+#define CAP_NET_BIND_SERVICE 10
+#define CAP_NET_BROADCAST 11
+#define CAP_NET_ADMIN 12
+#define CAP_NET_RAW 13
+#define CAP_IPC_LOCK 14
+#define CAP_IPC_OWNER 15
+#define CAP_SYS_MODULE 16
+#define CAP_SYS_RAWIO 17
+#define CAP_SYS_CHROOT 18
+#define CAP_SYS_PTRACE 19
+#define CAP_SYS_PACCT 20
+#define CAP_SYS_ADMIN 21
+#define CAP_SYS_BOOT 22
+#define CAP_SYS_NICE 23
+#define CAP_SYS_RESOURCE 24
+#define CAP_SYS_TIME 25
+#define CAP_SYS_TTY_CONFIG 26
+#define CAP_MKNOD 27
+#define CAP_LEASE 28
+#define CAP_AUDIT_WRITE 29
+#define CAP_AUDIT_CONTROL 30
+#define CAP_SETFCAP 31
+#define CAP_MAC_OVERRIDE 32
+#define CAP_MAC_ADMIN 33
+#define CAP_SYSLOG 34
+#define CAP_WAKE_ALARM 35
+#define CAP_BLOCK_SUSPEND 36
+#define CAP_AUDIT_READ 37
+#define CAP_PERFMON 38
+#define CAP_BPF 39
+#define CAP_CHECKPOINT_RESTORE 40
+
+#define VFS_CAP_REVISION_2 0x02000000
+#define VFS_CAP_FLAGS_EFFECTIVE 0x000001
+
+struct vfs_cap_data {
+  uint32_t magic_etc;
+  struct {
+    uint32_t permitted;
+    uint32_t inheritable;
+  } data[2];
+};
+#endif
 
 /* Rules for directories and files has moved to system/code/libcutils/fs_config.c */
 
-- 
2.50.1 (Apple Git-155)

